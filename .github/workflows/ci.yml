name: Room Sensor CI

on:
  push:
    branches: [main]
    paths-ignore:
      - 'docs/**'
      - '**.md'
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  BUILD_TYPE: Debug

jobs:
  static-analysis:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Install cppcheck
        run: sudo apt-get update && sudo apt-get install -y cppcheck cmake

      - name: Configure host tests (for compile_commands.json)
        run: cmake -S . -B build-host -DBUILD_HOST_TESTS=ON -DCMAKE_BUILD_TYPE=${{ env.BUILD_TYPE }}

      - name: Run cppcheck on Application/User
        run: >
          cppcheck --quiet --error-exitcode=1
          --enable=warning,performance,portability
          --suppress=unmatchedSuppression
          --inline-suppr
          --std=c11
          --language=c
          -I Application/User/App/Inc
          -I Application/User/Drivers/veml7700/Inc
          -I Application/User/Drivers/display/Inc
          -I Application/User/Platform/Inc
          -I Application/User/Storage/Inc
          -I Application/User/Telemetry/Inc
          -I Application/User/Communication/Inc
          -I Application/User/Command/Inc
          -I Application/User/Common/Inc
          --suppress=missingIncludeSystem
          Application/User/

  firmware-build:
    runs-on: ubuntu-24.04
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4

      - name: Install ARM GCC toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake gcc-arm-none-eabi binutils-arm-none-eabi

      - name: Configure firmware
        run: >
          cmake -S . -B build-fw
          -DCMAKE_BUILD_TYPE=${{ env.BUILD_TYPE }}
          -DCMAKE_TOOLCHAIN_FILE=cmake/gcc-arm-none-eabi.cmake

      - name: Build firmware
        run: cmake --build build-fw --target room_sensor --parallel $(nproc) 2>&1

      - name: Firmware size
        run: arm-none-eabi-size build-fw/room_sensor.elf

      - name: Generate artifacts
        run: |
          arm-none-eabi-objcopy -O binary build-fw/room_sensor.elf build-fw/room_sensor.bin
          arm-none-eabi-objcopy -O ihex build-fw/room_sensor.elf build-fw/room_sensor.hex

      - name: Upload firmware artifacts
        uses: actions/upload-artifact@v4
        with:
          name: room-sensor-firmware
          path: |
            build-fw/room_sensor.elf
            build-fw/room_sensor.bin
            build-fw/room_sensor.hex
          if-no-files-found: error

      # P2: stack-usage reporting + guard. The firmware is built with
      # -fstack-usage, producing one *.su file per translation unit. We report
      # the top-15 project-owned functions by static (frame-only) stack usage,
      # excluding vendor HAL/BSP noise (under cmake/stm32cubemx). The guard
      # fails when a single project-owned function reports a static frame
      # exceeding the stated threshold. LIMITATION: per-function static usage
      # is NOT the call-chain (worst-case total) stack; it deliberately ignores
      # dynamic/call usage, which is a stack-summing analysis outside this gate.
      - name: Report stack usage (top-15 project-owned)
        run: |
          su_files=$(find build-fw -name '*.su' 2>/dev/null || true)
          if [ -z "$su_files" ]; then
            echo "WARN: no -fstack-usage .su files produced; skipping report"
            exit 0
          fi
          echo "== top-15 project-owned static stack usage (bytes static = frame only) =="
          for f in $su_files; do
            # .su line: "<file>:<line>:<col>:<func>\t<bytes>\tstatic"
            awk 'NF >= 3 && $3 == "static" {
              # keep rows whose source is under Application/User (project-owned)
              if (index($1, "Application/User") > 0) {
                bytes=$2; f=$1;
                sub(/^.*:[0-9]+:[0-9]+:/, "", f);   # strip file:line:col -> func
                print bytes"\t"f
              }
            }' "$f"
          done | sort -rn | head -15 | awk '{printf "%6d  %s\n", $1, $2}'
      - name: Fail CI if a project-owned function exceeds stack threshold
        run: |
          # Justified threshold (bytes, static frame only). The largest
          # project-owned static frames are the command/response path
          # (Command_Run, HandleGetManifest) ~1152 B at -O0; 2048 leaves
          # margin while still catching pathological regressions.
          THRESHOLD=2048
          worst=$(find build-fw -name '*.su' 2>/dev/null | while read f; do
            awk 'NF >= 3 && $3 == "static" && index($1, "Application/User") > 0 { print $2 }' "$f"
          done | sort -rn | head -1)
          if [ -n "$worst" ] && [ "$worst" -gt "$THRESHOLD" ]; then
            echo "ERROR: project-owned static frame of ${worst} B exceeds ${THRESHOLD} B"
            exit 1
          fi
          echo "PASS: worst project-owned static frame = ${worst:-0} B (<= ${THRESHOLD} B)"

  portable-core-build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Install tools
        run: sudo apt-get update && sudo apt-get install -y cmake gcc

      - name: Build portable core (no ARM toolchain)
        run: >
          cmake -S . -B build-core
          -DCMAKE_BUILD_TYPE=${{ env.BUILD_TYPE }}
          -DBUILD_HOST_PLATFORM=ON
          && cmake --build build-core --parallel $(nproc) 2>&1

  host-integration-tests:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Install tools
        run: sudo apt-get update && sudo apt-get install -y cmake gcc libc6-dev

      - name: Build host tests
        run: cmake -S . -B build-host -DBUILD_HOST_TESTS=ON -DCMAKE_BUILD_TYPE=${{ env.BUILD_TYPE }}
          && cmake --build build-host --parallel $(nproc)

      - name: Run host tests
        run: ctest --test-dir build-host --output-on-failure --timeout 30

  sanitizers:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Install tools
        run: sudo apt-get update && sudo apt-get install -y cmake gcc libc6-dev

      - name: Build host tests with ASan + UBSan
        run: >
          cmake -S . -B build-san
          -DBUILD_HOST_TESTS=ON
          -DCMAKE_BUILD_TYPE=Debug
          -DCMAKE_C_FLAGS="-fsanitize=address,undefined -fno-omit-frame-pointer"
          -DCMAKE_EXE_LINKER_FLAGS="-fsanitize=address,undefined"
          && cmake --build build-san --parallel $(nproc)

      - name: Run full host suite under sanitizers
        run: ctest --test-dir build-san --output-on-failure --timeout 60

  policy-checks:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Check no dynamic memory in Application/User
        run: |
          if grep -rn 'malloc\|calloc\|realloc\|free' Application/User/ --include='*.c' --include='*.h' | grep -v 'test/'; then
            echo "ERROR: Dynamic memory found in Application/User"
            exit 1
          fi
          echo "PASS: No dynamic memory in Application/User"

      - name: Check no HAL_Delay in portable modules
        run: |
          BAD=$(grep -rln 'HAL_Delay' Application/User/App/ Application/User/Drivers/ Application/User/Communication/ Application/User/Command/ Application/User/Telemetry/ Application/User/Storage/ 2>/dev/null || true)
          if [ -n "$BAD" ]; then
            echo "ERROR: HAL_Delay found in portable modules:"
            echo "$BAD"
            exit 1
          fi
          echo "PASS: No HAL_Delay in portable modules"

      - name: Check no stm32 HAL include in portable modules
        run: |
          BAD=$(grep -rln 'stm32g4xx_hal\.h' Application/User/App/ Application/User/Drivers/ Application/User/Communication/ Application/User/Command/ Application/User/Telemetry/ Application/User/Storage/ 2>/dev/null || true)
          if [ -n "$BAD" ]; then
            echo "ERROR: stm32g4xx_hal.h included in portable modules:"
            echo "$BAD"
            exit 1
          fi
          echo "PASS: No stm32 HAL include in portable modules"

      - name: Check no HAL I2C/flash/GPIO handle types in portable modules
        run: |
          BAD=$(grep -rln 'I2C_HandleTypeDef\|GPIO_TypeDef\|HAL_StatusTypeDef\|HAL_FLASH\|HAL_IWDG\|HAL_Delay\|HAL_GetTick\|HAL_I2C' Application/User/App/ Application/User/Drivers/ Application/User/Communication/ Application/User/Command/ Application/User/Telemetry/ Application/User/Storage/ 2>/dev/null || true)
          if [ -n "$BAD" ]; then
            echo "ERROR: STM32 HAL symbols leaked into portable modules:"
            echo "$BAD"
            exit 1
          fi
          echo "PASS: No HAL symbols leaked into portable modules"

      - name: Check no secrets in telemetry/command responses
        run: |
          if grep -rn '"password"\|"token"\|"secret"\|"credential"' Application/User/Telemetry/ Application/User/Command/ --include='*.c' 2>/dev/null; then
            echo "ERROR: Secrets may be serialized"
            exit 1
          fi
          echo "PASS: No secrets serialized"